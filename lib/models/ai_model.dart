class AiModel {
  const AiModel({
    required this.id,
    required this.name,
    required this.description,
    required this.isLocal,
  });

  final String id;
  final String name;
  final String description;
  final bool isLocal;
}
